Ternary operatörüde if bloklarına alternatif kullanılabilir.

Örneğin;

```c#
int x = 20, y = 10;

var result = x > y ? "Doğruysa burası çalışır" : "yanlış ise burası";

Console.WriteLine(result);
```